$(function($) {
	$('.owl_banner').owlCarousel({
		items: 1,
	    loop:true,
	    margin:0,
	    nav: false,
	    smartSpeed :900
	});

	// $('.btn_contactus').click(function() {
 //        $('html, body').animate({
	//         scrollTop: $( $(this).attr('href') ).offset().top - 50
	//     }, 2000);
	//     return false;
 //    });

});